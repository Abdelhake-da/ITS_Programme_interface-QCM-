# ITS Programme — QCM Interface

Multiple-choice (QCM) exam interface for an Intelligent Tutoring System — PFE project.

## Structure
- `exam_pfe/` — Django project (exam management, question handling)

## Setup
```bash
pip install -r requirements.txt
python manage.py runserver
```
